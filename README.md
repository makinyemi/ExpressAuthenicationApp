# ExpressAuthenicationApp

This is an Express application that uses JWT Authentication to sign up new user, login existing users, generate Access and Refresh token, refresh access tokens, and sign out users.

